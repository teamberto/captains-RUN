{
  "name": "captain-go",
  "version": "1.0.0",
  "private": true,
  "description": "Captain Go - 3D temple lane runner",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "ios:sync": "npm run build:web && npx cap sync ios",
    "ios:open": "npm run ios:sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
